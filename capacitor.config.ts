import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gestao_reposicao.cobeb',
  appName: 'Cobeb | Gestão de Reposição',
  webDir: 'dist'
};

export default config;
